---
title: Testimonials
description: A testimonials section with a three-column card grid, avatar initials, and quoted text.
---

<TestimonialsBlockDemo />

## Installation

This block uses the following components. Add any you don't have yet:

```bash
npx radk add avatar badge card
```

## Usage

```tsx
import { TestimonialsBlock } from "@/components/blocks/testimonials"
```

```tsx
<TestimonialsBlock />
```

## Source

Edit the `testimonials` array to add your own quotes, names, and roles. Swap `AvatarFallback` with `AvatarImage` to show real photos.
