---
title: Stats
description: A stats section displaying key metrics in a responsive four-column card grid.
---

<StatsBlockDemo />

## Installation

This block uses the following components. Add any you don't have yet:

```bash
npx radk add badge card
```

## Usage

```tsx
import { StatsBlock } from "@/components/blocks/stats"
```

```tsx
<StatsBlock />
```

## Source

Edit the `stats` array to set your own values, labels, and descriptions. For live data, replace the static values with props or fetched data.
