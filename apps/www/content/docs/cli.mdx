---
title: CLI
description: Use the krux CLI to add components to your project.
---

The `krux` CLI lets you initialize your project and add components from the registry.

## init

Initialize krux in a new project:

```bash
npx krux init
```

You will be asked a few questions:

```
Where is your global CSS file? › app/globals.css
Configure the components alias: › @/components
Configure the utils alias: › @/lib/utils
```

This creates a `components.json` file and installs the base dependencies.

### Options

| Option | Description |
|--------|-------------|
| `-y, --yes` | Skip prompts, use defaults |
| `-c, --cwd <dir>` | Working directory (default: `process.cwd()`) |

## add

Add a component to your project:

```bash
npx krux add button
```

You can add multiple components at once:

```bash
npx krux add button card dialog select
```

The CLI will:

1. Fetch the component from the registry
2. Resolve and fetch dependencies
3. Install npm packages
4. Write component files to `src/components/ui/`

### Options

| Option | Description |
|--------|-------------|
| `-y, --yes` | Skip confirmation |
| `--dry-run` | Preview what would be installed without writing files |
| `-c, --cwd <dir>` | Working directory |

### Dry run

Preview what a component add would do:

```bash
npx krux add button --dry-run
```

## diff

Check if a locally installed component is up to date with the registry:

```bash
npx krux diff button
```

Output:

```
  button: up to date
  card: differs from registry
  Run `krux add --yes card` to update
```
